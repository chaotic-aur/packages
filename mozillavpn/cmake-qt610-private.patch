--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -147,6 +147,10 @@
     Xml
     Svg
 )
+
+# Ensure Qt 6 private targets are defined (Qt 6.10+ requires explicit find of *Private components)
+find_package(Qt6 COMPONENTS GuiPrivate QmlPrivate REQUIRED)
+
 include("scripts/cmake/check_qt_breakage.cmake")
 
 if(NOT ${CMAKE_SYSTEM_NAME} STREQUAL "Emscripten" AND
