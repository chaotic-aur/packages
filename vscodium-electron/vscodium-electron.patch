diff --git a/build.sh b/build.sh
index ccec4361..71640c71 100755
--- a/build.sh
+++ b/build.sh
@@ -14,7 +14,10 @@ if [[ "${SHOULD_BUILD}" == "yes" ]]; then
 
   cd vscode || { echo "'vscode' dir not found"; exit 1; }
 
-  export NODE_OPTIONS="--max-old-space-size=${MAX_OLD_SPACE_SIZE}"
+  #export NODE_OPTIONS="--max-old-space-size=${MAX_OLD_SPACE_SIZE}"
+  _target=$(</usr/lib/$_electron/version)
+  mkdir -p build/linux/${_vscode_arch}
+  curl -L https://github.com/electron/electron/releases/download/v$_target/SHASUMS256.txt > build/linux/${_vscode_arch}/electron.sha256sums
   export VSCODE_PUBLISH_COUNTER=1
 
   npm run gulp vscode-min-prepack
