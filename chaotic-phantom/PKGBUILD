pkgbase='phantom'
pkgname=(
  zapret-common-git
  zapret-docs-git
  zapret-nfqws-git
  zapret-tpws-git
)
pkgver=0.0.1
pkgrel=1
pkgdesc="metapackage - pending delete"
arch=('any')

for _p in "${pkgname[@]}"; do
  eval "package_$_p() {
    :
  }"
done
