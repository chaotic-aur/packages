# workaround for sums mismatch
unset b2sums
for i in "${source[@]}"; do
  b2sums+=('SKIP')
done

# prevent timeout from waiting for user input
eval "$(declare -f prepare | sed -E -e 's&^(\s*)[^#]*\bmake\b(.*)\bconfig\b.*$&&')"
