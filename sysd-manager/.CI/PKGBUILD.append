unset changelog
unset install

_units=$(($(nproc) > 16 ? $(nproc) : 16))
export RUSTFLAGS="-C opt-level=2 -C codegen-units=$_units -C lto=off"

options=('!lto')

eval "$(declare -f package | sed -e '/policy/s&./target/loc/&sysd-manager-proxy/data/&')"
